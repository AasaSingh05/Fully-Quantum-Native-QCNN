# Minimum versions known to work. For an exact, reproducible environment use
# requirements-lock.txt (the versions the reported results were produced with).
pennylane >= 0.38.0
pennylane-lightning >= 0.38.0
numpy >= 1.23.0
scikit-learn >= 1.0.0
matplotlib >= 3.5.0
seaborn >= 0.11.0
scipy >= 1.8.0
pandas >= 1.4.0
Pillow >= 9.0.0
# Optional extras:
#   tensorflow                            # only for the optional Keras CNN baseline
#   pennylane-qiskit qiskit-ibm-runtime   # only for experiments/hardware_run.py
